# FileIyagi (파일이야기) v1.4.0

Windows와 Linux에서 사용할 수 있는 **빠르고 가벼운 파일 관리자**입니다.

빠르고 반응성이 뛰어난 파일 관리자로, **한글 파일이름 변경과 검색 시 텍스트 입력이 안정적**으로 작동하도록 설계되었습니다.

고속 탐색, 키보드 중심 조작, 부드러운 썸네일 보기를 제공합니다.
인터페이스는 시스템 언어에 따라 자동으로 전환되어 자연스러운 사용 경험을 제공합니다.

---

## ✨ 주요 기능

### 탐색
* **브레드크럼 경로 표시줄** — 경로를 블록으로 표시, 클릭으로 상위 폴더 이동
* **경로 직접 입력** — `Ctrl+L`로 편집 모드 전환, `~` 홈 디렉토리 지원
* **경로 복사** — 현재 폴더 블록 클릭 시 전체 경로 클립보드 복사
* **앞으로/뒤로** — 마우스 4·5번 버튼, 툴바 ←→ 버튼으로 히스토리 탐색
* **즐겨찾기 사이드바** — 홈·바탕화면·문서 등 기본 폴더 + 사용자 북마크
* **장치 섹션** — 드라이브·USB 마운트 자동 표시 (볼륨 라벨 우선, Windows·Linux 공통)
* **마운트 자동 감지** (Linux) — USB 연결·해제 시 사이드바 자동 갱신
* **내 장치** — 사이드바에서 "내 장치"를 클릭하면 마운트되지 않은 드라이브까지 전체 목록 표시, 클릭으로 즉시 마운트
* **최근 파일 (RECENT)** — 폴더별 최근 열람 파일 목록, 탐색 시 자동 갱신
* **최근 경로 히스토리** — 경로표시줄 드롭다운에 최근 방문한 경로 15개 저장; 앱 재시작 후에도 유지

### 듀얼 패인 (2창 모드)
* **듀얼 패인 토글** — 상태바 맨 오른쪽 ◫ 버튼; 왼쪽=현재 패널, 오른쪽=홈(~/) 시작
* **마지막 경로 저장·복원** — 각 패인의 마지막 방문 경로를 세션 간에 유지
* **세션 간 상태 유지** — 2창 ON/OFF 상태가 앱 재시작 후에도 그대로 복원

### 보기
* **자세히 보기** — 이름·크기·확장자·수정일 컬럼, 폴더 우선 정렬, 컬럼 클릭 정렬
* **아이콘 보기** — 소(96px)·중(150px)·대(224px) 세 단계, 썸네일 자동 표시
* **썸네일** — 이미지(JPEG·PNG·GIF 등) 및 동영상(`ffmpegthumbnailer`) 자동 생성
* **폴더별 보기 모드 저장** — 폴더마다 마지막 보기 모드 기억
* **숨김 파일 토글** — `Ctrl+H` 또는 "." 버튼
* **폰트 크기 조절** — `Alt+휠`로 실시간 조절 (7~24pt)
* **보기 모드 전환** — `Ctrl+휠` 또는 `Ctrl+1~4`
* **정렬 버튼** — 상태표시줄의 이름·크기·확장자·날짜 버튼으로 빠른 정렬 전환
* **칼럼 크기 고정** — 크기·날짜·확장자는 고정 너비, 이름만 나머지 공간 채움 (드래그로 조절 불가)

### 검색
* **항상 표시 검색창** — 툴바 오른쪽에 항상 활성화
* **하위 폴더 재귀 검색** — 현재 폴더 기준 전체 하위 탐색
* **300ms 디바운스** — 입력 즉시 실행하지 않고 자연스럽게 시작
* **검색 중 파일 열기** — 검색 결과에서 파일을 열어도 검색 유지
* **검색 결과 드래그** — 검색 결과 파일을 다른 앱으로 드래그 가능
* **검색 결과 칼럼 레이아웃** — 이름·위치 컬럼이 나머지 공간을 절반씩 나눔; 크기·확장자·날짜는 고정 너비

### 파일 작업
* **복사 (F5)** — 즐겨찾기·장치 사이드바가 포함된 대상 폴더 선택 다이얼로그; 대기 중인 항목은 X 버튼으로 목록에서 제거 가능; 현재 복사 중인 항목은 X 버튼 자동 숨김
* **이동 (F6)** — 즐겨찾기·장치 사이드바가 포함된 대상 폴더 선택 다이얼로그
* **삭제 (Del, F8)** — 확인 대화상자 후 삭제; `Shift+Del`은 확인 없이 즉시 삭제
* **이름 변경 (F2)** — 인라인 편집, 확장자 제외 이름만 자동 선택
* **새 폴더 (F7)** — 어디서나 (사이드바 포커스 포함)
* **새 파일** — 현재 폴더에 빈 파일 생성 (툴바 버튼 또는 우클릭 메뉴); 툴바 버튼 우클릭으로 선호 에디터 앱 변경 가능
* **잘라내기 (Ctrl+X)** — 파일을 반투명으로 표시; 붙여넣기 시 이동
* **복사 (Ctrl+C)** — 파일을 클립보드에 복사
* **붙여넣기 (Ctrl+V)** — 잘라내기한 파일은 이동, 복사한 파일은 복제; 사이드바·툴바 등 어디에 포커스가 있어도 파일 목록 클릭 없이 바로 동작
* **다른 앱으로 열기** — MIME 기반 앱 목록 + 추천 앱; 마지막 사용 앱 기억
* **파일 뷰어 (F3)** — 텍스트·이미지 전용 읽기 전용 내장 뷰어; 최대화 버튼 지원
* **파일 편집기 (F4)** — 내장 텍스트 편집기; 선택 없이 F4 누르면 새 파일 생성; 최대화 버튼 지원; 드래그 선택 시 자동 스크롤 속도 제한으로 편안한 조작
* **파일 비교 (Ctrl+D)** — 두 번째 파일 선택 후 File A / File B / Diff 3탭 비교 뷰
* **빠른 미리보기 (F11)** — 선택 파일의 이미지·텍스트를 슬라이드인 패널로 표시
* **터미널 열기 (F9)** — 현재 폴더에서 터미널 실행; 툴바 버튼 우클릭으로 선호 터미널 앱 변경 가능
* **드래그 앤 드롭** — 파일을 다른 앱(이미지 뷰어, GIMP 등)으로 드래그; 드래그 고스트가 실제 컬럼 너비에 맞춤
* **드래그 앤 드롭 규칙** — 기본 드래그=이동(같은/다른 드라이브 무관), Ctrl+드래그=복사, Shift+드래그=복제(자동 이름 증가: `file (1).txt`), 우클릭 드래그=컨텍스트 메뉴(이동/복사/취소); 드래그 중 커서 피드백(이동 화살표 / 복사 ➕ / 불가 🚫); 상태바에 실시간 "이동: file.txt → 폴더명" 표시
* **속성** — 파일 이름·경로·크기·수정일; 폴더는 직접 파일 수 + 전체 재귀 파일 수 표시; 권한(chmod) 변경 가능
* **압축 (Ctrl+우클릭 → 압축...)** — zip·tar.gz·tar.bz2·tar.xz 형식 지원
* **압축 해제** — 아카이브 더블클릭 시 이름 폴더 생성 후 자동 해제·이동

### 툴바 사용자정의 버튼
* **사용자정의 버튼 3개** — 제목 표시줄의 터미널 버튼 옆에 위치
* **우클릭으로 설정** — 각 버튼에 이름·명령어·아이콘 지정; 아이콘은 `.desktop` 파일에서 자동 로드 및 저장
* **앱 선택 다이얼로그** — 설치된 앱 전체를 검색하여 선택 (표준 경로 + Snap + Flatpak); 명령어에 `%f`를 넣으면 실행 시 선택된 파일 경로로 치환; `%f` 없으면 현재 폴더를 자동으로 뒤에 추가
* **새 파일·터미널 앱 변경** — 각 버튼 우클릭으로 동일한 앱 선택 다이얼로그에서 선호 앱 변경; 저장되어 다음 클릭 시 자동 적용

### 시스템
* **창 크기·위치 저장** — 닫으면 저장, 다음 실행 시 복원
* **즐겨찾기 저장** — OS 표준 설정 경로에 저장 (Windows: %APPDATA%, Linux: ~/.config)
* **실시간 파일 감지** (Linux) — inotify 기반, 새 파일 즉시 목록 반영
* **실행 파일 감지** — ELF 매직 바이트 + 쉬뱅(#!) 검사 (NTFS 드라이브에서도 정확히 동작)
* **44개 언어 UI** — 시스템 로케일에 맞는 자동 번역 인터페이스

### 주요 기능 요약

| | |
|---|---|
| ◫ **듀얼 패인** | 상태바 ◫ 버튼으로 2창 모드 토글, 마지막 경로 복원 |
| 🖼 **썸네일 보기** | 이미지·동영상 XDG 캐시 기반 자동 생성 |
| 🔍 **빠른 검색** | 하위 폴더 재귀 검색, 항상 표시 검색창 |
| ⌨ **한글 IME 완벽 지원** | Qt6 네이티브 입력, 파일명 변경·검색 모두 정상 |
| 📁 **폴더별 보기 모드** | 각 폴더가 마지막 보기 방식을 기억 |
| ✂ **잘라내기/복사/붙여넣기** | 반투명 시각 표시 포함 전체 클립보드 파일 조작 |
| 🖱 **마우스 앞/뒤 버튼** | 자연스러운 히스토리 탐색 |
| 💾 **창 크기 자동 저장** | 닫으면 저장, 다시 열면 복원 |
| 🌐 **44개 언어 자동 전환** | 시스템 로케일에 따라 UI 언어 자동 선택 |
| 🗜 **압축/해제** | zip·tar.gz·7z·rar 더블클릭 해제, 선택 후 압축 |
| 💿 **내 장치** | 사이드바에서 미마운트 드라이브 확인 및 마운트 |
| 📂 **폴더별 최근 파일** | 사이드바 하단에 현재 폴더 최근 파일 목록 |
| ⚡ **빠른 로딩** | Nautilus보다 빠름 |
| 🖥 **기본 파일관리자** | xdg-mime 등록, 마우스 파일관리자 키 지원 |
| 🔧 **사용자정의 툴바 버튼** | 앱 선택기, `%f` 파일 인자, 아이콘 저장 지원 버튼 3개 |
| 🕑 **경로 히스토리 유지** | 최근 방문 경로 15개, 재시작 후에도 유지 |
| 🖱 **스마트 드래그 앤 드롭** | 기본=이동, Ctrl=복사, Shift=복제, 우클릭=컨텍스트 메뉴 |

---

## 🎮 단축키

| 키 | 기능 |
|---|---|
| F2 | 이름 변경 (이름만 선택, 확장자 제외) |
| F3 | 파일 뷰어 (읽기 전용, 텍스트·이미지) |
| F4 | 파일 편집기 (내장 텍스트 편집기; 선택 없으면 새 파일) |
| F5 | 대상 폴더로 복사 |
| F6 | 대상 폴더로 이동 |
| F7 | 새 폴더 |
| F8 | 삭제 (확인 있음) |
| F9 | 현재 폴더에서 터미널 열기 |
| F10 | 앱 닫기 |
| F11 | 빠른 미리보기 패널 토글 |
| Del | 삭제 (확인 있음) |
| Shift+Del | 삭제 (확인 없음) |
| Backspace | 상위 폴더 |
| Enter | 열기 / 폴더 진입 |
| Ctrl+L | 경로 직접 입력 토글 |
| Ctrl+H | 숨김 파일 토글 |
| Ctrl+F | 검색창 포커스 |
| Ctrl+R | 새로고침 |
| Ctrl+D | 파일 비교 (File A / File B / Diff 3탭) |
| Ctrl+X | 잘라내기 |
| Ctrl+C | 클립보드 복사 |
| Ctrl+V | 클립보드 붙여넣기 |
| Ctrl+1 | 자세히 보기 |
| Ctrl+2 | 아이콘 보기 (소) |
| Ctrl+3 | 아이콘 보기 (중) |
| Ctrl+4 | 아이콘 보기 (대) |
| Ctrl+휠 | 보기 모드 순환 |
| Alt+휠 | 폰트 크기 조절 |
| Alt+1~9 | 북마크 1~9번으로 바로 이동 |
| 마우스 4번 | 뒤로 |
| 마우스 5번 | 앞으로 |
| Esc | 검색 초기화 |

---

## 🖧 썸네일 설정

동영상 썸네일을 사용하려면 `ffmpegthumbnailer`를 설치하세요.

```bash
# Linux
sudo apt install ffmpegthumbnailer
```

## 🖥 지원 플랫폼

* Windows 10 / 11 (MinGW 또는 MSVC, Qt 6.4+)
* Ubuntu 22.04 / 24.04 이상 (GNOME Wayland / X11, Qt 6.4+)

---

## 👤 개발자

IYAGI INC
Email: [iyagicom@gmail.com](mailto:iyagicom@gmail.com)
GitHub: https://github.com/iyagicom

---

## 📜 라이선스
Copyright (c) 2026 IYAGI INC. All rights reserved.

이 소프트웨어는 실행 파일 형태로만 제공되며, 소스 코드는 공개되지 않습니다.

리눅스 버전:
개인, 상업, 교육, 정부, 단체 등 모든 목적으로 자유롭게 사용, 설치, 패키징 및 재배포할 수 있습니다.

윈도우 버전:
Microsoft Store를 통해 배포되며, 사용 및 라이선스는 MS Store에서 관리됩니다.

